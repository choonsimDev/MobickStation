import styled from "styled-components";

const StyledTopMidWraper = styled.div`
    width: 1200px;
    height : 100px;
    margin-top: 20px;
    background-color: pink;
`;


export default function AdArea() {
    return (
        <StyledTopMidWraper>AdArea</StyledTopMidWraper>
    );
}