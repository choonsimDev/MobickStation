import styled from "styled-components";

const StyledTopMidWraper = styled.div`
    width: 1200px;
    height : 600px;
    margin-top: 20px;
    background-color: gray;
`;

export default function MultiSection() {
    return (
        <StyledTopMidWraper>MultiSection</StyledTopMidWraper>
    );
}