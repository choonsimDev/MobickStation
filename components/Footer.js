import styled from "styled-components";

const StyledFooterWrapper = styled.div`
  width: 1200px;
  height: 450px;
  margin-top: 20px;
  background-color: lightgray;
`;

export default function Footer() {
  return <StyledFooterWrapper>Footer</StyledFooterWrapper>;
}
